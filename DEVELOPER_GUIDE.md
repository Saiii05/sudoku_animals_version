# Developer-Friendly Output

This guide provides the necessary information and code snippets to start developing the Animal Sudoku game.

## 1. Recommended File/Folder Structure

Here is the recommended structure for the game's assets and code:

```
animal-sudoku/
├── assets/
│   ├── icons/
│   │   ├── lion.svg
│   │   ├── elephant.svg
│   │   ├── monkey.svg
│   │   ├── panda.svg
│   │   ├── tiger.svg
│   │   ├── rabbit.svg
│   │   ├── owl.svg
│   │   ├── frog.svg
│   │   └── cat.svg
│   ├── sounds/
│   │   ├── place.mp3
│   │   ├── correct.mp3
│   │   └── win.mp3
│   └── images/
│       └── background.png
├── index.html
├── style.css
├── app.js
├── puzzles.js
├── animals.json
├── GAME_CONCEPT.md
├── ICON_DESCRIPTIONS.md
└── DEVELOPER_GUIDE.md
```

## 2. JSON Array of All 9 Animals

This JSON data (from `animals.json`) maps the animal names to their corresponding icon files.

```json
[
  {
    "id": 1,
    "name": "Lion",
    "icon": "assets/icons/lion.svg"
  },
  {
    "id": 2,
    "name": "Elephant",
    "icon": "assets/icons/elephant.svg"
  },
  {
    "id": 3,
    "name": "Monkey",
    "icon": "assets/icons/monkey.svg"
  },
  {
    "id": 4,
    "name": "Panda",
    "icon": "assets/icons/panda.svg"
  },
  {
    "id": 5,
    "name": "Tiger",
    "icon": "assets/icons/tiger.svg"
  },
  {
    "id": 6,
    "name": "Rabbit",
    "icon": "assets/icons/rabbit.svg"
  },
  {
    "id": 7,
    "name": "Owl",
    "icon": "assets/icons/owl.svg"
  },
  {
    "id": 8,
    "name": "Frog",
    "icon": "assets/icons/frog.svg"
  },
  {
    "id": 9,
    "name": "Cat",
    "icon": "assets/icons/cat.svg"
  }
]
```

## 3. Sample HTML/CSS/JS Structure for Rendering Icons

This section provides a basic structure for rendering the Sudoku grid with the animal icons.

### HTML (`index.html`)

```html
<div class="sudoku-grid" id="sudoku-grid">
    <!-- Cells will be generated by JavaScript -->
</div>
<div class="animal-palette" id="animal-palette">
    <!-- Animal icons will be loaded here -->
</div>
```

### CSS (`style.css`)

```css
.sudoku-cell img, .animal-icon img {
    width: 100%;
    height: 100%;
    object-fit: contain;
    user-drag: none; /* Prevents dragging the image */
    -webkit-user-drag: none;
}
```

### JavaScript (`app.js`)

The `app.js` file contains the core logic for the game. It handles fetching the animal data, creating the Sudoku grid, populating the animal palette, and managing user interactions like selecting and placing animals.

Refer to the `app.js` file for the full implementation.

## 4. Rules for Replacing Numbers with SVG Icons

1.  **Mapping:** Use the `id` field in the `animals.json` array to map the numbers (1-9) from the `puzzleGrid` and `solvedGrid` arrays to the corresponding animal.
2.  **Rendering:** When a cell in the grid has a non-zero number, find the animal object in the `animals` array with the matching `id`.
3.  **Icon Source:** Use the `icon` property (e.g., `assets/icons/lion.svg`) from the matched animal object as the `src` for an `<img>` element within the cell.
4.  **Empty Cells:** If a cell in the `puzzleGrid` has a value of `0`, it should be rendered as an empty, clickable cell.
5.  **User Input:** When a player places an animal in a cell, the game logic should update an internal representation of the grid with the corresponding animal `id`.
